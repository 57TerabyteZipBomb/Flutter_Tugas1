import 'package:flutter/material.dart';

class ExampleMoblie extends StatelessWidget {
  const ExampleMoblie({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('mobile page')),
      body: Center(child: Text('mobile example'),)
    );
  }
}